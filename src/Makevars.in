$(info The name of the shared library to be created is: $(SHLIB))

CXX_STD = CXX11

PKG_CXXFLAGS = -I. @OPENCL_FLAGS@ @SIMD_FLAGS@ -DRBUILD

PKG_LIBS = @RCPPPAR_LIB@ @OPENCL_LIB@

SOURCES = mds.cpp \
	        RcppExports.cpp \
	        factory.cpp \
	        backend/opencl/instantiate.cpp

OBJECTS = mds.o \
	        RcppExports.o \
	        factory.o \
	        backend/opencl/instantiate.o

all: $(SHLIB)
$(SHLIB): mds.o

